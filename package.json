{
  "name": "site-stitcher",
  "version": "1.0.0",
  "description": "The tool I use to build my website, picostitch.com",
  "main": "index.js",
  "scripts": {
    "test": "mocha -r esm src/*spec.js",
    "start": "npm run build && npm run serve",
    "serve": "http-server _output -d -i",
    "dev:start": "npm run build; npm run serve & ./build-on-file-change.sh",
    "dev:test": "npm run test -- --watch --bail",
    "postdev:start": "pkill -f http-server",
    "build": "mkdir -p _output; cp src/templates/* _output/; node -r esm src/index.js"
  },
  "keywords": [
    "static site generator",
    "website"
  ],
  "author": "Wolfram Kriesing",
  "license": "MIT",
  "devDependencies": {
    "esm": "^3.2.25",
    "hamjest": "^3.3.1",
    "http-server": "^0.12.1",
    "mocha": "^7.1.1"
  },
  "dependencies": {
    "marked": "^0.8.2",
    "tundrajs": "^1.0.0"
  }
}
