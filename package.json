{
  "name": "site-stitcher",
  "version": "1.0.0",
  "description": "The tool I use to build my website, picostitch.com",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && http-server _output -d -i",
    "dev:start": "npm run start & ./build-on-file-change.sh",
    "postdev:start": "pkill -f http-server",
    "build": "mkdir -p _output; cp src/index.html _output"
  },
  "keywords": [],
  "author": "Wolfram Kriesing",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.12.1"
  }
}
